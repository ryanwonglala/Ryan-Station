# Font Licenses

- **Noto Serif SC** (`noto-serif-sc-600-subset.woff2`) — SIL Open Font License 1.1. Subset to a fixed weight-600 (SemiBold) static instance and reduced to the CJK glyphs actually used on the site (headline characters only) via fontTools `varLib.instancer` + `pyftsubset`. Source: Google Fonts (`google/fonts` repo, `ofl/notoserifsc`). Full OFL 1.1 text: https://openfontlicense.org
